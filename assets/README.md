Place your hero image file here and name it `hero.jpg` so the site will pick it up automatically.

Steps:
1. Save the photo you attached as `hero.jpg` in this folder.
2. Open the site via a local server (e.g. `python -m http.server 8000`) and refresh the homepage.

If you want me to add an admin control to upload and set this image automatically, tell me and I'll implement it.